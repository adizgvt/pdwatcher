enum SyncStatus {
  pending,
  syncing,
  success,
  failed
}

enum ServiceMethod {
  get,
  post,
  put,
  delete,
  download,
  upload
}

enum FileChangeEnum {
  files,
  filesDeleted,
  shareFiles
}